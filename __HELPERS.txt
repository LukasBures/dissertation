
# kill procesu na GPU
nvidia-smi | grep 'python' | awk '{ print $5 }' | xargs -n1 kill -9

# modifikace odberu W na GPU
sudo nvidia-smi -i 0 -pl 140 # 1080
sudo nvidia-smi -i 1 -pl 250 # 1080Ti

#  2TB disk mount point
cd /media/lukas/WD_2TB

